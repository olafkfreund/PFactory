"""Spec-layout helpers for the build CLI.

Minimal package: only what ``cli.utils`` imports. AIFactory carries a much
larger ``spec/`` package (pipeline, orchestrator, phases); PFactory is the
planner and needs none of it, so this is deliberately not a port of that one.
"""
