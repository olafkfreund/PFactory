name: verification-core drift gate

# Blocking drift gate for the hub shared libraries this repo vendors.
#
# PFactory hand-vendors two modules from the Factory hub canonical (scripts/):
#   apps/backend/runners/artifact_store.py       (RFC-0016 artifact store)
#   apps/backend/plan/emit/cost_router_core.py   (RFC-0014 cost router)
#
# Until Factory#400 these were vendored with NO gate of any kind, so nothing
# detected them diverging — and they had, for weeks. Both were missing real
# behaviour: artifact_store lacked the whole RFC-0017 workspace layer, the tar
# path-traversal guards and the MinIO role= tagging that makes evidence
# retention fire; cost_router_core lacked admit_within_budget.
#
# These copies MUST stay byte-identical to the hub canonical. To change one:
# land it in the hub canonical first (CODEOWNERS-reviewed), re-vendor byte-for-
# byte, and bump HUB_PIN_SHA. Never edit a vendored copy to satisfy a local
# style bar — they are excluded from ruff in ruff.toml precisely so the local
# formatter cannot silently break byte-exactness.
#
# The gate is named "verification-core" because it reuses that hub checker,
# which implements exactly this mechanism. Its name is narrower than its job;
# renaming it means touching every consumer's workflow, tracked separately.

on:
  pull_request:
    branches: [dev, main]
    # NO paths: FILTER, deliberately (Factory#525). This used to list the
    # vendored copies, which was correct while the gate only compared bytes --
    # it had nothing to say unless one of those paths changed, and Factory#400
    # is the story of that list being incomplete.
    #
    # The stray-copy guard (Factory#523) inverted the requirement. It exists to
    # catch a vendored module appearing at a path NOBODY HAS LISTED: a fresh
    # copy, or one left behind when a mapping was removed. Such a PR touches
    # none of the listed paths by definition, so the filter switched the gate
    # off in exactly the case the guard was written for, and the unmapped copy
    # landed on the default branch unseen.
    #
    # The list was also a second, hand-maintained copy of the hub's
    # SERVICE_LAYOUTS living in a workflow file -- the fork-with-a-citation
    # shape Factory#483 exists to undo. Its history here is a changelog of
    # being patched one module at a time, always after the fact.
    #
    # The gate is a few seconds of Python against a checkout. Running it on
    # every PR costs almost nothing and removes a whole class of "the check did
    # not run" -- which on a green PR page is indistinguishable from "the check
    # passed" (Factory#471).
  push:
    branches: [dev, main]

concurrency:
  group: vcore-drift-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Pinned Factory hub commit carrying the canonical modules. Bump deliberately
  # when re-vendoring (see header).
  #
  # Bumped for olafkfreund/Factory#714 (py/tarslip): artifact_store.py now always
  # extracts with the stdlib `data` filter instead of falling back to an
  # unfiltered extractall on pre-3.12 runtimes. This is the SQUASH-MERGE commit
  # on hub `main`, never a PR-branch head (Factory#638).
  HUB_PIN_SHA: "11834afc75523324d95448ae6ce07eeca907c63b"

jobs:
  drift:
    name: vendored copies match the hub canonical (byte-exact)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out this service
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: service

      - name: Assert the hub pin is really on hub main
        # HUB_PIN_SHA comes from THIS workflow file, which on a `pull_request`
        # trigger is the PR's own copy. Fork commits are fetchable by SHA from
        # Factory itself (one shared object store), so without this a fork PR
        # could pin a canonical tree crafted to match its own drifted vendored
        # copy and this gate would diff two matching trees and go green
        # (Factory#806).
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: service/.github/scripts/assert-hub-pin-on-main.sh "${HUB_PIN_SHA}" "verification-core-drift HUB_PIN_SHA"

      - name: Check out the Factory hub canonical at the pinned SHA
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: olafkfreund/Factory
          ref: ${{ env.HUB_PIN_SHA }}
          path: factory-hub
          # If the Factory hub is private, provide a read token here.

      - name: Check out the hub drift CHECKER at main
        # Not at HUB_PIN_SHA: a gate fetched from its own pin cannot ship its
        # own fix. The CANONICAL above stays pinned, so comparisons remain
        # reproducible; only the gate's own logic floats (Factory#405).
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: olafkfreund/Factory
          ref: main
          path: factory-hub-main

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Run the hub drift gate against this service (blocking)
        run: |
          python factory-hub-main/scripts/check_verification_core_drift.py \
            --canonical factory-hub/scripts \
            --service pfactory \
            --root service
